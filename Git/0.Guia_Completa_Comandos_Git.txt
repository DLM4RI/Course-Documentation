
============================================================
                GUÍA COMPLETA DE COMANDOS GIT
============================================================

-----------------------------------------------
🔹 CONFIGURACIÓN INICIAL
-----------------------------------------------
git config --global user.name "Tu Nombre"
git config --global user.email "tu_email@example.com"
git config --global core.editor "code --wait"
git config --list
git config --local ...

-----------------------------------------------
🔹 CREACIÓN Y CLONACIÓN DE REPOSITORIOS
-----------------------------------------------
git init
git clone <url>
git clone -b <rama> <url>
git clone --depth 1 <url>

-----------------------------------------------
🔹 ESTADO E HISTORIAL
-----------------------------------------------
git status
git log
git log --oneline --graph --decorate
git log -p
git show <commit>
git diff
git diff --staged

-----------------------------------------------
🔹 AÑADIR Y CONFIRMAR CAMBIOS
-----------------------------------------------
git add <archivo>
git add .
git commit -m "Mensaje"
git commit -am "Mensaje"
git commit --amend
git reset HEAD <archivo>
git reset --soft HEAD~1
git reset --hard HEAD~1

-----------------------------------------------
🔹 RAMAS (BRANCHES)
-----------------------------------------------
git branch
git branch -r
git branch -a
git branch <nombre>
git branch -d <nombre>
git branch -D <nombre>
git checkout <nombre>
git checkout -b <nombre>
git switch <nombre>
git switch -c <nombre>

-----------------------------------------------
🔹 FUSIÓN Y SINCRONIZACIÓN
-----------------------------------------------
git merge <rama>
git merge --abort
git rebase <rama>
git rebase --continue
git fetch
git fetch --all
git pull
git pull --rebase
git push
git push -u origin <rama>
git push origin --delete <rama>
git push --tags

-----------------------------------------------
🔹 REMOTOS
-----------------------------------------------
git remote -v
git remote add origin <url>
git remote rename origin nuevo-nombre
git remote remove origin
git remote set-url origin <url>

-----------------------------------------------
🔹 RESTAURAR / DESHACER CAMBIOS
-----------------------------------------------
git checkout -- <archivo>
git restore <archivo>
git restore --staged <archivo>
git reset <archivo>
git reset --soft HEAD~1
git reset --hard HEAD~1
git revert <commit>

-----------------------------------------------
🔹 STASH (GUARDAR TRABAJO TEMPORALMENTE)
-----------------------------------------------
git stash
git stash list
git stash pop
git stash apply stash@{n}
git stash drop
git stash clear
git stash show -p stash@{0}

-----------------------------------------------
🔹 ETIQUETAS (TAGS)
-----------------------------------------------
git tag
git tag -l "v1.*"
git tag <nombre>
git tag -a <nombre> -m "mensaje"
git push origin <nombre>
git push --tags
git tag -d <nombre>
git push origin :refs/tags/<nombre>

-----------------------------------------------
🔹 LIMPIEZA
-----------------------------------------------
git clean -n
git clean -f
git gc
git prune
git fsck

-----------------------------------------------
🔹 INSPECCIÓN AVANZADA
-----------------------------------------------
git blame <archivo>
git shortlog -sn
git reflog
git ls-tree HEAD
git cat-file -p <hash>
git rev-parse HEAD
git grep "texto"

-----------------------------------------------
🔹 SINCRONIZACIÓN DE RAMAS
-----------------------------------------------
git checkout rama1
git merge master
git pull origin master
git rebase master

-----------------------------------------------
🔹 SUBMÓDULOS
-----------------------------------------------
git submodule add <url> <ruta>
git submodule init
git submodule update
git submodule update --remote
git submodule sync

-----------------------------------------------
🔹 COMANDOS DE DEPURACIÓN
-----------------------------------------------
git bisect start
git bisect bad
git bisect good <commit>
git bisect reset

-----------------------------------------------
🔹 COMANDOS DE SEGURIDAD Y FIRMA
-----------------------------------------------
git verify-commit <commit>
git verify-tag <tag>
git config --global commit.gpgsign true
git config --global user.signingkey <clave>

-----------------------------------------------
🔹 ALIADOS Y ATAJOS
-----------------------------------------------
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status

-----------------------------------------------
🔹 INFORMACIÓN DEL SISTEMA GIT
-----------------------------------------------
git help
git version
git rev-list --count HEAD
git count-objects -v
git show-ref
git describe --tags

-----------------------------------------------
🔹 COMANDOS DE SSH Y CREDENCIALES
-----------------------------------------------
ssh-keygen -t ed25519 -C "tu_email@example.com" -f ~/.ssh/id_github
ssh-add ~/.ssh/id_github
ssh-add -l
ssh -T git@github.com

-----------------------------------------------
🔹 COMANDOS ÚTILES VARIOS
-----------------------------------------------
git archive --format=zip HEAD -o repo.zip
git mv <archivo> <nuevo_nombre>
git rm <archivo>
git rm --cached <archivo>

-----------------------------------------------
🔹 COMANDOS PARA EQUIPOS Y COLABORACIÓN
-----------------------------------------------
git fetch origin pull/<ID>/head:<rama>
git cherry-pick <commit>
git remote show origin
git pull origin <rama>
git push origin <rama>

============================================================
                    FIN DEL DOCUMENTO
============================================================
