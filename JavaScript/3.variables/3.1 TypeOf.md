# 3.1 TypeOF

---

**TypeOF** es un operador (es algo que veremos más adelante "operador").

Se usa para **determinar el tipo de dato de una variable**.

---

## 📖 ANTES: ¿QUÉ ES UNA SINTAXIS?

Es ese **conjunto de reglas** que determinan cómo se define o debe escribir en un lenguaje para que sea entendido de una manera correcta.

---

### 💡 EJEMPLO:
```javascript
let nombre = "Ana";
```

**Desglose:**

- `let` → palabra clave para declarar una variable
- `nombre` → nombre de la variable
- `=` → operador de asignación
- `"Ana"` → valor asignado
- `;` → indica el fin de la instrucción (opcional en JavaScript, pero recomendable)

---

## ¿Cómo se usa TypeOf?

### Sintaxis:
```
typeof + valor
```

---

### 📋 Características:

- **valor** puede ser una variable, un número, una cadena, un objeto, etc.
- El resultado es **siempre una cadena (string)** que indica el tipo.

---

## 📝 EJEMPLOS:
```javascript
let numero = 42;
console.log(typeof numero); // "number"

let texto = "Hola";
console.log(typeof texto); // "string"

let bandera = true;
console.log(typeof bandera); // "boolean"

let objeto = { nombre: "Ana" };
console.log(typeof objeto); // "object"

let indefinido;
console.log(typeof indefinido); // "undefined"

let funcion = function() {};
console.log(typeof funcion); // "function"
```

---

## 🖥️ ¿Qué es `console.log`?

`console.log` es una función para ejecutar el código en la **Consola**. Para verlo hay 2 maneras:

---

### 1️⃣ Como profesional - Desde el IDE (Visual Studio Code)

**Pasos:**

1. Abre tu consola en Visual Studio Code
2. Arriba a la derecha
3. Click al icono de un cuadrado con una línea horizontal algo así:
```
_______________
|             |
|             |
|_____________|
|_____________|
```

4. Se te abrirá la consola
5. Para este paso tendrás que tener **Node** instalado, el cual es el "entorno de ejecución de JS"
   
   > (Ve al curso instalaciones y ahí aprenderás)

6. Escribe: `node nombre-de-tu-archivo.js`
7. Y si todo sale bien deberías ver tu código ejecutarse

---

### 2️⃣ Fácil - Desde el navegador

**Pasos:**

1. Abre tu archivo HTML en tu navegador de confianza
2. Presiona la tecla **F12**, en algunos PCs se debe presionar **Alt + F12**
3. Se te desplegará un cuadro con el código HTML de tu página
4. Buscarás en la parte superior algo como **"Console"** o **"Consola"**
5. Ahí se ejecutará todo tu código

---

## 🎯 Uso de TypeOf

**TypeOf** tiene muchos usos, más que todo para **validar qué datos se usan en un programa** y saber qué hacer luego...

---

**Escrito por "Viper."**

---

**-----------------> Siguiente clase**