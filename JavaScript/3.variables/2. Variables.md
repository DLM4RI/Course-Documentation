# 2. Variables

---

En el 99% de lenguajes de programación te vas a encontrar esto: **"variables"**, pero ¿qué son?

---

## 🤔 ¿QUÉ SON?

Las variables o **declaración de variables** es esa forma en la que se crea un espacio para guardar un dato. ¿Cómo así?

**Piensa en una cajita donde guardarás información que usarás luego...**

---

### 💡 Ejemplo práctico:

Tu padre quiere arreglar una silla, necesita un martillo, el cual tiene en su **caja de herramientas**... 

¿Cuál es la variable? 

**La caja de herramientas**, ya que almacenó el martillo por un largo tiempo para ser usado cuando lo necesite, en este caso arreglar la silla.

---

## 📦 Concepto de Bloques o "Scope"

Antes de conocer cómo funcionan las variables debes entender que la programación en JS es por **bloques** o por **"scope"**... ¿Cómo así?

Un **bloque** es todo lo que está entre llaves `{}`.

De manera técnica, **Scope** significa **ámbito o alcance**. Es desde dónde puedes acceder (o no) a una variable.

---

### Ejemplo de bloque:
```javascript
{
  "Hola"
}
```

El `"Hola"` está dentro de las llaves, con lo cual hace parte de ese bloque.

---

### Variable fuera del bloque:
```javascript
"hola"

{

}
```

El `"hola"` pasaría a ser parte del **bloque global**, es decir **"variable general"** que se puede usar dentro de cualquier bloque creado.

---

## 2.1 Tipos de Variables

### ¿Cómo se declaran las variables?
```
tipo de variable  +  nombre de variable  +  operador de asignación  =  valor a asignar
```

**Ejemplo:**
```javascript
let nombre = "Juan"
```

---

## 2.1 Existen 3 tipos o maneras de declarar una variable

### `LET` • `CONST` • `VAR`

---

### 2.1.1 VAR

Es la manera más **antigua** de declarar una variable. Ya no es tan utilizada, porque fue remplazada por su predecesora **"LET"**.

**VAR** sencillamente es para designar una variable **"global"** en cualquier bloque, y así poder cambiar su valor en cualquier momento sin importar el bloque.

---

### 2.1.2 LET

Es el **estándar utilizado actualmente**, ya que esta respeta los bloques `{}`. Es decir, si asignas una variable en un bloque, esta variable no podrá ser usada fuera de este.

---

#### 📌 Ejemplo 1:
```javascript
let objeto = "tijera"

{
  document.write(objeto) // --> aquí le decimos escribe en
                         // el documento html la variable
                         // objeto, la cual tiene como dato
                         // "tijera"
}
```

> ✅ **Esto funciona** ya que `objeto` fue declarado con un `let`, el cual se encuentra en el bloque global, y al estar ahí puede ser usado en cualquier otro bloque.

---

> **¿Por qué se escribe `"tijera"` y no `tijera`?**
> 
> Eso lo aprenderás en el curso... (tipos de datos)

---

#### 📌 Ejemplo 2:
```javascript
{
  let objeto = "tijera"
}

console.log(objeto)
```

> ❌ **Esto NO funciona** ya que `objeto` fue declarado con un `let`, el cual se encuentra en el bloque cerrado, y al estar ahí no puede ser usado fuera de ese bloque, solo dentro de ese bloque o dentro de un bloque creado dentro del bloque en el que está.

---

#### 📌 Ejemplo 3 (funciona):
```javascript
{
  let objeto = "tijera"

  {
    document.write(objeto) 
  }
}
```

> ✅ **Esto SÍ funciona** porque el bloque interno tiene acceso al bloque externo donde se declaró la variable.

---

### 2.1.3 CONST

`const` se usa para declarar variables **cuyo valor no debe cambiar**. Una vez le das un valor, no puedes ponerle otro.

`const` también tiene respeto por bloques...

---

#### 📌 Ejemplo:
```javascript
const nombre = "Carlos"

nombre = "mario" // --> Reasignamos el valor, es decir,
                 // decimos, nombre ya no será 
                 // "Carlos" sino "mario"

document.write(nombre)
```

> ❌ **ESTO NO FUNCIONARÁ Y DARÁ ERROR**
> 
> **"CONST NO PERMITE REASIGNAR EL VALOR"**

---

### ⚠️ Nota importante sobre CONST:

`const` evita que cambies la **referencia** de la variable, pero **no impide modificar el contenido** si es un objeto o arreglo........

> (En próximas clases veremos eso. No te estreses ni te adelantes, recuerda que el curso está hecho para aprender progresivamente....);

---

**Escrito por "Viper."**

---

**-----> PRÓXIMA CLASE**