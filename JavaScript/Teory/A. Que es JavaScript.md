# A. ¿Qué es JS?

---

## JavaScript es un lenguaje INTERPRETADO...

### ¿CÓMO ASÍ?

#### En los lenguajes interpretados:

El código se lee y ejecuta línea por línea por otro programa llamado **intérprete**, en este caso **"el navegador"**.

---

### 💡 Piensa en algo como:

Es como si leyeras una receta mientras cocinas, paso por paso.

---

### ⚙️ Lenguaje compilado (lo contrario)

Lo contrario a esto es un **lenguaje compilado** que lo que hace es que antes de ejecutarse, se convierte en un archivo binario que entiende directamente la computadora...

---

## SIGAMOS CON EL CONCEPTO DE JavaScript

**JavaScript** es un lenguaje de programación usado para darle **interactividad y dinamismo** a las páginas web. Permite que los sitios no sean solo texto e imágenes, sino que reaccionen cuando el usuario hace clic, escribe o interactúa.

---

## ALGO MÁS TÉCNICO SERÍA:

JavaScript es un lenguaje de programación **interpretado**, de **alto nivel** y **multiparadigma**, y **débilmente tipado**.

### 🔄 ¿Multiparadigma?

Que permite usar múltiples formas de programación:

- **Programación orientada a objetos (POO)**
  - Trabajar con clases y objetos.

- **Programación funcional**
  - Usar funciones puras, inmutabilidad, etc.

- **Programación imperativa**
  - Dar instrucciones paso a paso.

- **Programación declarativa**
  - Decir qué quieres lograr sin detallar los pasos.

---

### 🏷️ ¿TIPADO?

Cuando dices que un lenguaje es **tipado**, es porque debes especificar el tipo de dato (lo veremos más adelante), es decir, decirle al lenguaje: "ey esto es un **'número'**, esto es un **'texto'**, esto es etc. etc."

Al decir que JavaScript es **débilmente tipado**, decimos que en el 90% de los casos **no se especifica el tipo de dato**...

> **No te enredes con esto, lo aprenderás poco a poco.**

---

## 📝 AHORA UN PEQUEÑO QUIZ :D

<details>
<summary><strong>1. ¿QUÉ ES UN LENGUAJE COMPILADO?</strong></summary>

Un lenguaje compilado es aquel en el que el código fuente se traduce completamente a código máquina (binario) antes de ejecutarse. Este proceso lo realiza un programa llamado **compilador**, generando un archivo ejecutable que la computadora puede entender directamente sin necesidad de un intérprete.

**Ejemplo:** C, C++, Go, Rust.

</details>

<details>
<summary><strong>2. ¿QUÉ SIGNIFICA INTERPRETADO?</strong></summary>

Un lenguaje interpretado es aquel en el que el código se lee y ejecuta **línea por línea** por un programa llamado **intérprete** (en el caso de JavaScript, el navegador). No se genera un archivo binario previo, sino que se traduce y ejecuta en tiempo real.

**Es como leer una receta mientras cocinas, paso a paso.**

</details>

<details>
<summary><strong>3. ¿QUÉ ES JavaScript?</strong></summary>

JavaScript es un **lenguaje de programación interpretado, de alto nivel, multiparadigma y débilmente tipado** que se usa principalmente para darle **interactividad y dinamismo** a las páginas web. Permite que los sitios reaccionen a las acciones del usuario (clics, escritura, interacciones, etc.).

</details>

---

**Practica las respuestas de estas preguntas.** El éxito de un programador no está en hacer código sino en saber conceptos...

---

**Escrito Por "Viper."**

**Siguiente Parte --->**